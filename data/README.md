# Data

`panel_cleaned.csv` is the aligned modeling panel used in the saved forecasting experiment. It contains the transformed GDP target and public macroeconomic predictors collected from FRED.

The reproducible script in `src/run_forecasting_experiment.py` downloads the public FRED series and rebuilds this cleaned panel.

